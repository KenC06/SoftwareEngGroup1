# How to run
Assuming you have ruby installed already:
`cd server`
`bundle install`
`bin/rails server -p 3001`
In another terminal:
`cd client`
`npm install`
`npm run start`
Then navigate to http://localhost:3000/
